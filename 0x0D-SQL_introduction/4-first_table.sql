-- creates a first_table in the current database
-- id int
-- name character
-- database name is passes as an argument of the command

CREATE TABLE IF NOT EXISTS first_table (id INT, name VARCHAR(256));
