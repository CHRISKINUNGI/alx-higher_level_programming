-- script to compute the average records in second_table
-- the result column name should be average
-- database name will be passed as an argument of sql command

SELECT AVG(score) AS average FROM second_table;